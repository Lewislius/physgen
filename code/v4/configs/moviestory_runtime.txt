# Reference only; install dependencies only on explicit user request, in moviestory.
# Preserve its existing CUDA 12.8 torch stack. Launchers never install dependencies.
--index-url https://pypi.org/simple
torch==2.8.0+cu128
torchvision==0.23.0+cu128
# W&B is optional and already available in the current environment.
